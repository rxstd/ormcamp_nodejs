<!DOCTYPE html>
<html>
  <head>
    <title>게시글 읽기</title>
  </head>
  <body>
    <style>
      label {
        display: inline-block;
        width: 100px;
      }

      input[type="text"],
      textarea {
        width: 300px;
        padding: 5px;
        border: 1px solid #ccc;
      }

      button {
        padding: 5px 10px;
        border: 1px solid #ccc;
        background-color: #fff;
        cursor: pointer;
      }
    </style>
    <h1>게시글 읽기</h1>
    <form action="/articles/create" method="post">
      <p>
        <label for="title">제목</label>
        <input
          type="text"
          name="title"
          id="title"
          readonly
          value="<%= article.title %>"
        />
      </p>
      <p>
        <label for="writer">작성자</label>
        <input
          type="text"
          name="writer"
          id="writer"
          readonly
          value="<%= article.writer %>"
        />
      </p>
      <p>
        <label for="content">내용</label>
        <textarea name="content" id="content" cols="30" rows="10" readonly>
<%= article.content %>
        </textarea>
      </p>
      <p>
        <button type="button" onclick="location.href='/articles'">목록</button>
        <button
          type="button"
          onclick="location.href='/articles/modify/<%= article.idx %>'"
        >
          수정
        </button>
        <button
          type="button"
          onclick="location.href='/articles/delete/<%= article.idx %>'"
        >
          삭제
        </button>
      </p>
    </form>
  </body>
</html>
